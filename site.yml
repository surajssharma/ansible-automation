---
- name: install httpd
  hosts: web
  remote_user: root
  become: true

  tasks:

    - name: install package
      dnf:
        name: httpd
        state: present

    - name: start service
      service:
        name: httpd
        state: started
        enabled: true
